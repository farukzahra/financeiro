{
  "name": "financeiro",
  "private": true,
  "version": "0.23.0",
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "test": "pnpm --filter @financeiro/api test && pnpm --filter @financeiro/web test:e2e",
    "build": "pnpm test && pnpm -r run build",
    "release:check": "node scripts/check-release.mjs --staged",
    "hooks:install": "git config core.hooksPath .githooks",
    "db:up": "docker compose up -d db",
    "db:down": "docker compose down",
    "db:generate": "pnpm --filter @financeiro/api db:generate",
    "db:migrate": "pnpm --filter @financeiro/api db:migrate",
    "db:seed": "pnpm --filter @financeiro/api db:seed",
    "test:e2e": "pnpm --filter @financeiro/web test:e2e"
  }
}
